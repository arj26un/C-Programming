//  area of rectangle by taking input from user
# include <stdio.h>
int main(){
    int length,breadth;
    printf("the length of rectangle is  ");
    scanf("%d", & length);
    printf("the breadth of rectangle is  ");
    scanf( "%d",& breadth);
    printf("the area of rectangle with length = %d and breadth = %d  \t is %d", length,breadth, length*breadth);

 return 0 ;
}