// The area of rectangle inputed by coder 
# include <stdio.h>
int main(){
     int length,breadth;
     length = 105;
     breadth =865;
     int area = length*breadth;
     printf(" \n the area of rectangle with length = %d and breadth = %d   \t i.e. %d*%d = %d\n", length,breadth,length,breadth,area);
      
 return 0;
}