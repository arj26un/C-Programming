// WAP to find area of square
# include <stdio.h>

int main(){
int side,square;
 printf("the side of aquare is =");
 scanf("%d",&side);
 square = side*side;
 printf("the area of square with side %d is %d", side,square);

 return 0 ;
}