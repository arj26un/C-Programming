// WAP first n natural number using for loop
#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number:");
    scanf("%d", &n);
    for (int i = 1; i <=n; i++)

    {
        printf("%d is the natural number\n", i);
        
    }
    return 0;
}